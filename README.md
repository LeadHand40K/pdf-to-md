# PDF to Markdown Converter

A lightweight Python script that converts PDF files to Markdown using **PyMuPDF (fitz)**.

## 🚀 Features
- Extracts readable text from PDFs  
- Supports multi-page documents  
- Zero dependencies besides PyMuPDF  
- Extremely simple to run  

---

## 📦 Installation

Install Python dependencies:

```bash
pip install pymupdf

##Usage

Place your PDF in the project folder, then run:

```bash
python pdf_to_md.py input.pdf output.md
